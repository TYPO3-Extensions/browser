plugin.tx_browser_pi1 {
  navigation {
      // plugins
    map =
    map {
        // empty statement for proper comments only
      plugins {
      }
        // masterclustering
      plugins  =
      plugins {
          // enabled
        mastercluster  =
        mastercluster {
          enabled = {$plugin.tx_browser_pi1.map.leafletcontrolling.masterclustering}
        }
      }
    }
  }
}