plugin.tx_browser_pi1 {
  navigation {
      // 140703: empty statement: for proper comments only
    map {
    }
      // configuration
    map =
    map {
        // 140703: empty statement: for proper comments only
      compatibility {
      }
        // mode
      compatibility =
      compatibility {
          // [string]: "leaflet (default)" or "oxMap (deprecated)"
        mode = {$plugin.tx_browser_pi1.map.compatibility.mode}
      }
    }
  }
}