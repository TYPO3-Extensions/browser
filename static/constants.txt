plugin.tx_browser_pi1 {

  # cat=Browser - Download Module/enable/100;   type=boolean;   label= Debugging: If enabled, the download module will prompt to the DRS (Development Reporting System)
  downloadmodule.drs = 0
  # cat=Browser - Download Module/enable/102;   type=int+;      label= page uid: Id of the download page
  downloadmodule.pageUid = 
  # cat=Browser - Download Module/enable/104;   type=string;    label= Uid parameter:Parameter for the uid of the document in the URL
  downloadmodule.urlParam = downloadUid 
  # cat=Browser - Download Module/enable/105;   type=string;    label= Uid of the download single view:Take it from your TypoScript. Default in case of the DAM sample is 37165
  downloadmodule.uidSingleView = 37165
  # cat=Browser - Download Module/dims/100;     type=int+;      label= Thumbnail list view - width: Max width for a thumbnail in the list view
  downloadmodule.thumbnailListViewMaxW = 80
  # cat=Browser - Download Module/dims/101;     type=int+;      label= Thumbnail single view - width: Max width for a thumbnail in the single view
  downloadmodule.thumbnailSingleViewMaxW = 240
  # cat=Browser - Download Module//100;          type=string;   label= field files: table.field with the files (lowercase!):Examples: tx_dam.file_name, tx_org_downloads.documents
  downloadmodule.tableField = tx_org_downloads.documents
  # cat=Browser - Download Module//200;          type=string;   label= Marker field uid: TABLE.FIELD with the uid (uppercase!) of your media record:Examples: TX_DAM.UID, TX_ORG_DOWNLOADS.UID
  downloadmodule.uid = TX_ORG_DOWNLOADS.UID 
  # cat=Browser - Download Module//201;          type=string;   label= Marker field files: TABLE.FIELD with the files (uppercase!):Examples: TX_DAM.FILE_NAME, TX_ORG_DOWNLOADS.DOCUMENTS
  downloadmodule.files = TX_ORG_DOWNLOADS.DOCUMENTS
  # cat=Browser - Download Module//202;          type=string;   label= Marker field path: TABLE.FIELD with the files (uppercase!):Examples: TX_DAM.FILE_PATH, TX_ORG_DOWNLOADS.DOCUMENTS_FROM_PATH
  downloadmodule.path = TX_ORG_DOWNLOADS.DOCUMENTS_FROM_PATH
  # cat=Browser - Download Module//203;          type=string;   label= Marker field titles: TABLE.FIELD with the titles (uppercase!):Examples: TX_DAM.TITLE, TX_ORG_DOWNLOADS.TITLE
  downloadmodule.titles = TX_ORG_DOWNLOADS.TITLE

  # cat=Browser - Image Dimensions/dims;   type=int+;    label= Single image max width: Max width for an image displayed in a single view
  singleMaxW = 240
  # cat=Browser - Image Dimensions/dims;   type=int+;    label= Single image max height: Max height for an image displayed in a single view
  singleMaxH = 180
  # cat=Browser - Image Dimensions/dims;   type=int+;    label= Single image maximum amount: Amount of maximum images per one record in a single view
  singleImageCount = 10
  # cat=Browser - Image Dimensions/dims;   type=int+;    label= List image pop-up max width: Max width for an image displayed in an extra pop-up window
  singlePopupMaxW = 1000m
  # cat=Browser - Image Dimensions/dims;   type=int+;    label= List image pop-up max height: Max height for an image displayed in an extra pop-up window
  singlePopupMaxH = 800
  # cat=Browser - Image Dimensions/dims;   type=int+;    label= List image max width: Max width for an image displayed in a list view
  listMaxW = 80
  # cat=Browser - Image Dimensions/dims;   type=int+;    label= List image max height: Max height for an image displayed in a list view
  listMaxH = 60
  # cat=Browser - Image Dimensions/dims;   type=int+;    label= List image maximum amount: Amount of maximum images per one record in a list view
  listImageCount = 1
  # cat=Browser - Image Dimensions/dims;   type=int+;    label= List image pop-up max width: Max width for an image displayed in an extra pop-up window
  listPopupMaxW = 500m
  # cat=Browser - Image Dimensions/dims;   type=int+;    label= List image pop-up max height: Max height for an image displayed in an extra pop-up window
  listPopupMaxH = 500

  # cat=Browser - jQuery//100;       type=options[apple,classic,default,default-rtl];  label= jQuery.plugin.jstree theme: Name of the theme of tree views
  jQuery.plugin.jstree.theme    = apple
  # cat=Browser - jQuery//201;       type=string;  label= jQuery.plugin.jstree selector 1: HTML id of div tag which contains a tree view
  jQuery.plugin.jstree.selector_01 = tx-browser-pi1-jstree-01
  # cat=Browser - jQuery//202;       type=string;  label= jQuery.plugin.jstree selector 2: HTML id of div tag which contains a tree view
  jQuery.plugin.jstree.selector_02 = tx-browser-pi1-jstree-02
  # cat=Browser - jQuery//203;       type=string;  label= jQuery.plugin.jstree selector 3: HTML id of div tag which contains a tree view
  jQuery.plugin.jstree.selector_03 = tx-browser-pi1-jstree-03
  # cat=Browser - jQuery//204;       type=string;  label= jQuery.plugin.jstree selector 4: HTML id of div tag which contains a tree view
  jQuery.plugin.jstree.selector_04 = tx-browser-pi1-jstree-04
  # cat=Browser - jQuery//205;       type=string;  label= jQuery.plugin.jstree selector 5: HTML id of div tag which contains a tree view
  jQuery.plugin.jstree.selector_05 = tx-browser-pi1-jstree-05

  # cat=Browser - Localisation/;       type=int+;    label= EN:Id of the sys_language record for the English language.
  typeNum.sys_language_en = 0
  # cat=Browser - Localisation/;       type=int+;    label= DE:Id of the sys_language record for the German language.
  typeNum.sys_language_de = 0
  # cat=Browser - Localisation/;       type=int+;    label= ES:Id of the sys_language record for the Spanish language.
  typeNum.sys_language_es = 0
  # cat=Browser - Localisation/;       type=int+;    label= FR:Id of the sys_language record for the French language.
  typeNum.sys_language_fr = 0
  # cat=Browser - Localisation/;       type=int+;    label= IT:Id of the sys_language record for the Italien language.
  typeNum.sys_language_it = 0

  # cat=Browser - Map//101;             type=boolean;   label= Enable the map:Please enable the map. Without enabling any map won't rendered. 
  map.enabled = 0 
  # cat=Browser - Map//102;             type=string;    label= table.field with record uid: Example: tx_org_headquarters.uid
  map.field.linktoSingle = 
  # cat=Browser - Map//103;             type=string;    label= table.field with the latitude: Example: tx_org_headquarters.latitude
  map.field.latitude = 
  # cat=Browser - Map//104;             type=string;    label= table.field with the longitude: Example: tx_org_headquarters.longitude
  map.field.longitude = 
  # cat=Browser - Map//105;             type=string;    label= table.field with the content: table.field with the content for the popup. Example: tx_org_headquarters.short
  map.field.description = 
  # cat=Browser - Map//106;             type=string;    label= table.field with the category: Example: tx_org_headquarterscat.title
  map.field.category = 
  # cat=Browser - Map//201;         type=int+;   label= Map height: Height of the map (in pixel)
  map.height = 400
  # cat=Browser - Map//202;         type=int+;   label= Map width: Width of the map (in pixel)
  map.width = 600
  # cat=Browser - Map//301;             type=options[auto,fixed];  label= Zoom level mode:Auto: An optimal zoom level will calculated. Fixed: Zoom level will taken from the field "Fixed zoom level" below.
  map.zoomLevel.mode = auto
  # cat=Browser - Map//302;             type=int+;  label= Fixed zoom level: The fixed zoom level. Value from 1 to 18. Value has an effect only, if zoom level mode is "fixed". Default: 4
  map.zoomLevel.fixed = 4
  # cat=Browser - Map//303;             type=int+;  label= Maximum zoom level: The maximum zoom level. Maximum value has an effect only, if zoom level mode is "auto".  Default: 18
  map.zoomLevel.max = 18
  # cat=Browser - Map//401;             type=string;    label= Path to legend icons: Path to the directory, which contains the images for the form legend (with ending slash!). Default: EXT:browser/res/js/map/icons/legend/ 
  map.pathToLegend = EXT:browser/res/js/map/icons/legend/
  # cat=Browser - Map//402;             type=string;    label= Path to point icons: Path to the directory, which contains the images for points in the map (with ending slash!). Default: EXT:browser/res/js/map/icons/points/ 
  map.pathToPoints = EXT:browser/res/js/map/icons/points/

  # cat=Browser - Navigation/;       type=string;  label= Record Browser: uid name (default: showUid)
  recordBrowser.uidName = showUid

  # cat=Browser - Page Objects//100;       type=int+;    label= AJAX page object: Type number (typenum) of the AJAX page object. Default: 28562
  typeNum.ajaxPageObj = 28562
  # cat=Browser - Page Objects//101;       type=int+;    label= CSV page object: Type number (typenum) of the CSV page object. Default: 29370
  typeNum.csvPageObj = 29370
  # cat=Browser - Page Objects//102;       type=int+;    label= Download page object: Type number (typenum) of the page object for downloads. Default: 31230
  typeNum.downloadPageObj = 31230
  # cat=Browser - Page Objects//103;       type=int+;    label= Page object for map category markers: Type number (typenum) of the map page object. Default: 32654
  typeNum.mapPageObj = 32654

  # cat=Browser - Relation Buildung/enable; type=boolean; label= SQL Relation Building Automatically: Try to build relations automatically? TRUE is recommended.
  autoSQLrelConfig = 1
  # cat=Browser - Relation Buildung/enable; type=boolean; label= SQL Relation Building Oneway Only: Use only relations from the local table to the foreign table? TRUE is recommended.
  autoSQLrelOnlyOneway = 1
  # cat=Browser - Relation Buildung/enable; type=boolean; label= SQL Simple Relation with Self References: Use self references in simple relations? FALSE is recommended.
  autoSQLrelSelfReference = 0
  # cat=Browser - Relation Buildung/enable; type=boolean; label= Discover Title: Try to discover fields with titles and wrap them as the header? Only for single views. TRUE is recommended.
  autoDiscoverTitle = 1
  # cat=Browser - Relation Buildung/enable; type=boolean; label= Discover Timestamps: Try to discover fields with timestamps and wrap them in a human readable format (See format in the section 'Others' below) ? TRUE is recommended.
  autoDiscoverTimestamp = 1
  # cat=Browser - Relation Buildung/enable; type=boolean; label= Discover Images: Try to discover fields with image file names, image capture, image alttext and image titletext. Wrap it as an image object? TRUE is recommended.
  autoDiscoverImage = 1
  # cat=Browser - Relation Buildung/enable; type=boolean; label= Discover Documents: Try to discover fields with document file names and wrap values as documents? TRUE is recommended.
  autoDiscoverDocument = 1


  # cat=Browser - Time Format//;       type=string;  label= Format for strftime
  strftime = %a, %d.%b.%y %H:%M

}
