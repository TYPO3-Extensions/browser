plugin.tx_browser_pi1 {
  navigation {
    map {
        // 140704, dwildt, empty statement for proper comment only.
      debugging {
      }
        // route
      debugging =
      debugging {
          // relations
        route =
        route {
            // [Boolean] Route Relations:(Recommended) Debug unproper relations for routes. You will get a prompt in the frontend, if categories or marker (POI) aren't proper
          relations = {$plugin.tx_browser_pi1.map.debugging.route.relations}
        }
      }
    }
  }
}